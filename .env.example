# MongoDB
MONGODB_URI=mongodb://localhost:27017/minha-arvore

# NextAuth
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Redis para Rate Limiting (escolha uma opção)
# Opção 1: Redis local/Docker (recomendado para desenvolvimento)
REDIS_URL=redis://localhost:6379

# Opção 2: Upstash Redis (recomendado para produção)
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

# Nota: Se nenhum Redis estiver configurado, o sistema usará cache em memória (Map)
# que não persiste entre restarts e não funciona em múltiplas instâncias
